# Supported Special-Series Ingestion

Closed review evidence for the first ingestion of Another AD02-03, IKS.gear IKS0-6, EKICO EC1-4 and アワメモ限定 OR0.

- Detail pages are the fact authority.
- List-page IDs are preserved as `wiki_no`; stable project IDs use `another`, `iks`, `ekico` and `awamemo` prefixes.
- Special templates were normalized by `pipeline/ingest/backfill_special_pool_semantics.py`.
- Review queue placeholders are closed as `confirmed` after component-level semantic review and Step1/Step2 validation.
